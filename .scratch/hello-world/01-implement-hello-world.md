# Implement `hello_world` function

Status: ready-for-agent

## Description

Write a `hello_world` function in `src/hello_world.py` that returns the string `"Hello, world!"`.

## Acceptance criteria

- Function `hello_world()` returns `"Hello, world!"`
- A corresponding test in `tests/test_hello_world.py` asserts the return value

## Implementation notes

Use `uv run pytest` to verify the test passes.